import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Territórios",
};

export default function TerritoriosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
